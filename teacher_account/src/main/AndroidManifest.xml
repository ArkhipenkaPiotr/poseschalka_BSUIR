<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.petarda.teachermarker.teachermarker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:name=".TeacherMarkerApplication"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".screen.subjects.SubjectsActivity">

        </activity>

        <activity android:name=".screen.authentication_base.AuthenticationActivity"
                  android:noHistory="true">
        </activity>

        <activity android:name=".screen.find_subject.FindSubjectsActivity">

        </activity>
    </application>

</manifest>